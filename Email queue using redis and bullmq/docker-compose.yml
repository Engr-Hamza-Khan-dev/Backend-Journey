
services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - email-queue-network

volumes:
  redis-data:

networks:
  email-queue-network:
    driver: bridge